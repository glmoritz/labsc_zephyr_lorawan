# Drivers for the expansion port. Without CONFIG_I2C the i2c node would be
# "okay" in the devicetree but no driver would ever run — and the pins would
# stay Hi-Z, silently doing nothing.
CONFIG_I2C=y
